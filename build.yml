platform: linux

image_resource:
  type: docker-image
  source: {repository: calico/go-build}

inputs:
- name: libcalico-go

run:
  path: ./libcalico-go/run-uts

image_resource:
  type: docker-image
  source:
    repository: calico/go-build
